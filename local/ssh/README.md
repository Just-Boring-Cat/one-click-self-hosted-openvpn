# SSH Keys

Place SSH keys for the target VPS here.

Expected default:

```text
local/ssh/id_ed25519
local/ssh/id_ed25519.pub
```

For this private workspace, the current Hetzner key is stored here under a project-specific name.
